# `npm run dev` keeps MSW fixtures available for mock UI/E2E work. For a real
# local demo, start the backend in DEMO_MODE and use `npm run dev:real`.
VITE_API_BASE=
VITE_DEMO_MODE=false
VITE_USE_MOCKS=false
